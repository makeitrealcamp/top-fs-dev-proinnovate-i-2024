function capitalize(string) {
  return string[0].toUpperCase() + string.slice(1);
}
console.log(capitalize('william'));

/*
  Convertimos el primer caracter de la cadena a mayuscula y lo concatenamos con el resto de la cadena
*/
